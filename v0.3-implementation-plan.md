# wechat-content-skill — v0.3 实施计划

> **依据：** [blueprint.md](blueprint.md)（v1 架构基线）+ [v0.2-implementation-plan.md](v0.2-implementation-plan.md) 第 7 节「明确不做」与第 8 节收尾承诺（标记嵌套、发布监控、规则迭代）
> **目标版本：** v0.3（语义标记嵌套、发布数据统计、humanize 规则迭代）
> **状态：** Approved

---

## 1. v0.3 唯一目标

在 v0.2「表达面 × 交付面 × 可度量」之上，补齐三块纵深：**表达力**（语义标记可受控嵌套，卡片内可放提示 / 引用）、**数据面**（发布后的群发数据可查询、可留档）、**质量规则的迭代性**（humanize 从 6 维扩到 9 维，规则集可版本化演进）。

衡量标准：**`:::card` 内嵌 `:::note` 等合法组合从解析到渲染到三层门禁全绿，非法嵌套仍被 parser 与 lint 双双拒绝；发布后某天的群发数据一条命令拉取并落盘回执；humanize v2 新维度可检出 v0.2 检不出的 AI 味，且既有 evals 基线零回归。**

### backlog 对照（v0.2「明确不做」→ v0.3 交付物）

| # | v0.2 明确不做 | v0.3 交付物 | 里程碑 |
|---|--------------|------------|--------|
| 1 | 语义标记嵌套（`:::card` 内嵌 `:::note` 等）——解析器大改 | 栈式解析器 + card 容器化 AST + 嵌套渲染 / lint / serialize / 修复定位全链路 | N1 |
| 2 | humanize 规则集大幅迭代（v0.2 仅校准阈值） | rules v2：3 个新检测维度 + 词表扩充 + evals 校准 | N3 |
| 3 | 监控告警（发布后数据回流）——仅 `status()` 查询，群发数据统计进 v0.3 | `DatacubeService`（getarticletotaldetail 封装）+ `scripts/stats.py` 回执留档 | N2 |
| 4 | Web UI / 复杂 CLI 交互 | 继续不做（推迟至 v0.4）：入口仍是 Agent（SKILL.md）+ `scripts/` | — |
| 5 | 性能优化、并发、缓存、i18n | 继续不做（推迟至 v0.4）：延续 v0.1/v0.2 决策 | — |

---

## 2. 关键技术决策

| 决策点 | 选择 | 理由（蓝图 / 既有决策依据） |
|--------|------|------------------------|
| 嵌套范围 | **registry 驱动的受控嵌套**：`ComponentSpec` 新增 `allowed_children: frozenset[str]`（默认空）；模块常量 `MAX_COMPONENT_DEPTH = 2`；v0.3 仅 `card` 开放（可嵌 note / quote / callout），禁止 card 嵌 card | 排版上唯一有意义的多块容器就是 card（v0.2 文档示例即「card 内嵌 note」）；note / quote / callout 是内联文本块，嵌套无排版意义。受控范围把「解析器大改」的风险压到最小；registry 是标记契约的唯一事实源（v0.1 决策），扩容不破契约。深度 2 = 容器 + 1 层子组件 |
| AST 表达 | `CardNode.items` 扩为 `list[str \| NoteNode \| QuoteNode \| CalloutNode]`；嵌套子组件 node_id 采用**子编号** `component_N.M` | 嵌套子组件仍是完整 AST 节点（type / props / node_id 齐全），定向修复按 H3 需要 node 级寻址；联合类型对旧数据（纯 str 列表）向后兼容，v0.2 fixtures 不变仍合法 |
| 解析器改造 | 显式**栈**替换当前两层 while；非法嵌套维持既有错误类型 `invalid_nesting` | 保留单遍行扫描与确定性（H8）；错误类型不变，既有测试语义可平移——`invalid_nesting` 的含义从「任何嵌套」收窄为「不允许的嵌套」（不在 `allowed_children` / 超深度 / 叶子组件内出现标记） |
| lint 同步 | lint 的单值开闭状态（`open_name/open_id/open_line/body`）改栈式；与 parser 共享 registry 的 `allowed_children` + 深度常量 | 防漂移：lint（容错、一次报全集）与 parser（fail-fast）对同一非法输入必须报同类型错误——v0.1 起的两层校验契约 |
| 嵌套渲染 | 子组件复用既有组件样式发射器（`_note_html` 等）；card 对节点 items 加间距包装；主题新增**可选**间距键，主题缺失时用内置默认值 | 零新增 CSS 属性（白名单约束不破）；5 主题无需强制改 YAML，渲染 eval 兜底 |
| serialize 往返 | card items 混排输出：str → `- 要点` 列表行；节点 → 嵌套标记块；属性转义规则不变 | 嵌套 AST `serialize → parse` 深度等价（round-trip 测试锁定）；扁平输入的输出格式逐字节不变 |
| 统计接口 | 封装**新接口** `getarticletotaldetail`（旧 `getarticletotal` 微信已停止维护） | 新接口字段更全（读完率 / 跳出位置 / 阅读来源分布 / 收藏 / 在看），且是官方推荐的替代 |
| 统计形态 | integrations 层 Service + frozen dataclass（同 `freepublish.py` 模式）+ `scripts/stats.py` CLI 回执留档 `outputs/stats/` | 查询型能力不进 pipeline 状态机（发布仍是显式人工动作）；不扩大 Artifact schema 表面 |
| humanize 迭代 | rules.yaml v1 → v2：新增 3 个 **warning 级**维度——连接词密度（connective_density）/ 句首单调（opening_monotony）/ 句长均匀度（sentence_length_uniformity）；`HumanizeRules`/`Limits` 新字段全部带默认值 | 三个维度均确定性可判（H8：Judge 永远是代码）；warning 级不推翻 v0.2 阈值基线；旧 rules YAML / fixtures 因默认值而兼容 |
| 版本 | `pyproject.toml` 0.2.0 → 0.3.0；打 tag `v0.3.0` | 语义化版本：新增能力、无破坏性变更（v0.2 全部 fixtures / examples / checkpoint 必须仍可加载） |

### 附：getarticletotaldetail 接口要点（N2 实现依据）

- `POST /datacube/getarticletotaldetail?access_token=...`，body `{begin_date, end_date}`（`YYYY-MM-DD`）；**仅支持 1 天跨度**（begin = end），end_date 最大昨日；错误码 61500（日期格式）/ 61501（范围超限）。
- 返回 `list[]`：`ref_date` / `msgid` / `publish_type`（0=已通知订阅用户，1=未开启通知）/ `title` / `content_url` / `detail_list[]`，另有 `is_delay`。
- `detail_list[]` 元素（按 stat_date 逐日）：`read_user`（阅读人数）、`read_user_source[]`（来源分布：全部 / 公众号消息 / 聊天会话 / 朋友圈 / 公众号主页 / 其他 / 推荐 / 搜一搜）、`share_user`（分享）、`zaikan_user`（爱心赞 / 在看）、`like_user`（拇指赞）、`comment_count` / `collection_user`（收藏）、`praise_money`（赞赏，单位分）、`read_subscribe_user`、`read_delivery_rate`（送达率）、`read_finish_rate`（读完率）、`read_avg_activetime`（平均阅读时长，分钟）、`read_jump_position[]`（1–5 对应 0–20% … 80–100% 跳出位置 + rate）。
- 约束：仅**认证账号**可用；数据自 2025-11-01 起存储；每篇文章统计其发表日起 30 天。

---

## 3. 里程碑与依赖关系

```text
N1 表达力：语义标记嵌套（parser / AST / lint / 渲染 / serialize / 修复定位 —— 本版最大改造）
   ↓（渲染 eval 新增嵌套样例依赖 N1）
N2 数据面：发布数据统计（独立于 N1，可并行）
N3 规则面：humanize v2（独立于 N1/N2，可并行；evals 校准自足）
   ↓
N4 文档面：native / publishing / content 子技能 + quality 规则 + README / 根 SKILL.md（依赖 N1–N3 定稿）
   ↓
N5 收尾：CHANGELOG / 版本 0.3.0 / 全量验证 / tag / 推送
```

- N1 是主线（改动横跨 5 个模块 + 主题 + evals）。
- N2、N3 互不依赖、也不依赖 N1，可随时并行插入。
- N4 必须等 N1–N3 行为定稿（文档零偏差要求）。
- N5 必须最后：全量回归 + 版本标记。

---

## 4. 任务分解

> 任务编号 `Nx-Ty`；每任务标注交付物与验收标准（DoD）。

### N1 — 表达力：语义标记嵌套

| ID | 任务 | 交付物 | DoD |
|----|------|--------|-----|
| N1-T1 | registry 嵌套契约 | `renderer/components/registry.py`：`ComponentSpec.allowed_children`（默认 `frozenset()`）；`MAX_COMPONENT_DEPTH = 2`；card → `{note, quote, callout}` | 既有 4 组件 spec 其余字段零变化；`card` 嵌 `card` 等非法组合在契约层即可判定 |
| N1-T2 | AST 容器化 | `renderer/ast/nodes.py`：`CardNode.items` 联合类型；`schemas/` 重新导出 | v0.2 fixtures（纯 str items）校验仍通过；JSON Schema 含联合定义 |
| N1-T3 | 栈式解析器 | `renderer/ast/parser.py`：组件扫描改显式栈；子组件 node_id = `component_N.M`（M 为容器内子组件序号）；违例（不在 allowed_children / 超深度 / 叶子组件内出现标记）→ `ParseError("invalid_nesting")` | card+note / card+quote / card+callout / 混排（str 与节点交错）解析成功；card 嵌 card、note 内嵌任何标记、深度 3 → invalid_nesting；**扁平输入行为与 v0.2 逐字节一致** |
| N1-T4 | serialize 往返 | parser.py `serialize`：items 混排输出（str → 列表行；节点 → 嵌套块）；属性转义不变 | 嵌套 AST `serialize → parse → serialize` 等价（round-trip 测试）；扁平输出格式不变（既有快照不回归） |
| N1-T5 | 嵌套渲染 | `renderer/html/renderer.py`：`_card_html` 支持节点 items（复用各组件发射器 + 间距包装）；主题可选间距键缺省回退 | 5 主题渲染嵌套样例全过 `lint_html` + `lint_gzh`；`render_segments` 仍按顶层节点输出（嵌套子节点并入父段） |
| N1-T6 | lint 栈式化 | `validators/component/lint.py`：单值状态 → 栈；嵌套违例报 `invalid_nesting`；EOF 时多层 unclosed 全量报告 | 与 parser 对同一非法输入报同类型错误（防漂移测试）；既有 lint 用例零回归 |
| N1-T7 | 修复定位扩展 | `core/workflow/repair.py`：节点寻址遍历 card items（子编号可定位） | 嵌套节点的 ValidationIssue 可映射到 AST 节点；修复循环语义不变（上限 3 轮、绝不整篇重写） |
| N1-T8 | 嵌套测试 + eval | `tests/unit/test_parser.py`（L172 嵌套用例改写为「合法嵌套 + 非法组合」双向）/ lint / renderer 快照；新增嵌套渲染 eval（5 主题 × 嵌套语料，入 `tests/evals/cases/`） | 全量绿；eval 报告含嵌套用例（用例数以实际运行报告核验，不预写数字） |

### N2 — 数据面：发布数据统计

| ID | 任务 | 交付物 | DoD |
|----|------|--------|-----|
| N2-T1 | 响应模型 | `integrations/wechat/datacube.py`：`ArticleTotalDetail` / `ArticleDailyDetail` / `ReadSource` / `JumpPosition` frozen dataclass | 字段覆盖「接口要点」全量返回；`asdict` 可直接 JSON 序列化 |
| N2-T2 | DatacubeService | 同文件：`article_stats(date)`（begin=end=date）+ `call_with_token_retry`；61500/61501 → `ProviderError` 语义化消息；未来日期客户端预检 | mock 下成功 / 空数据 / errcode / token 40001 重试四路径全测；CI 零网络 |
| N2-T3 | stats CLI | `scripts/stats.py`：`--date`（默认昨日）/ `--account`；终端摘要表 + `outputs/stats/<date>_article_stats.json` 回执留档 | 参数与根 SKILL.md 文档一致；重复运行幂等覆盖；无认证环境给出可行动的错误提示 |
| N2-T4 | mock 测试 | `tests/unit/test_datacube.py` | 四路径 + 日期预检 + msgid 拼装（msg_data_id_index）全覆盖 |
| N2-T5 | 文档与验证清单 | `skills/publishing/SKILL.md` 监控节；`docs/verification-checklist.md` 增补真实验证步骤 | 认证号要求 / 2025-11-01 起数据 / 30 天窗口 / 1 天跨度约束全部落档 |

### N3 — 规则面：humanize v2

| ID | 任务 | 交付物 | DoD |
|----|------|--------|-----|
| N3-T1 | 新检测维度 | `skills/content/humanize/detector.py`：connective_density（连接词密度 / 千字超阈）、opening_monotony（连续同句首 ≥ N 句）、sentence_length_uniformity（句长标准差过低），全部 warning 级 | 每维度确定性可判、单测覆盖正反例；句数不足时跳过（防误伤短文） |
| N3-T2 | rules v2 | `rules.yaml` version 1 → 2：新增 `connectives` 清单；黑名单 / 成对句式按 v0.2 事后观察扩充；`HumanizeRules`/`Limits` 新字段全带默认值 | 旧字段不删不改；v0.2 fixtures 的 HumanizeReport 仍合法；`load_rules` 对 v2 YAML 全量生效 |
| N3-T3 | eval 校准 | 跑全量 evals；新增负例语料（连接词堆砌文 / 句首单调文）；阈值调整集中在 `tests/evals/thresholds.py` | 既有语料基线不破（好文 ≥ 90、AI 味 ≤ 50、每类 ≥ 2 例）；新维度对既有好文零误伤（若误伤则校准 limits 而非放宽 pass_score） |
| N3-T4 | 文档 | `skills/content/SKILL.md`（humanize 规则节）+ `skills/quality/rules/content.md` | 维度清单（6 → 9）与 rules.yaml 一一对应；penalty / limits 数值与实现零偏差 |

### N4 — 文档面

| ID | 任务 | 交付物 | DoD |
|----|------|--------|-----|
| N4-T1 | 嵌套语法文档 | `skills/native/components/card.md`（+ note/quote/callout 各自补「可被 card 嵌套」说明）、`skills/native/SKILL.md`、`skills/quality/rules/component.md` | 语法示例、允许组合表、深度上限、错误类型全落档；示例可被 parser 实际解析（文档即测试） |
| N4-T2 | README / 根 SKILL.md | 能力清单反映 v0.3：嵌套标记、`scripts/stats.py`、humanize 9 维 | 与实现零偏差：CLI 参数、维度数、组件组合逐一与代码核对（吸取 v0.2「20 条语料」表述教训，数字以实际产物核验） |
| N4-T3 | publishing / content 子技能更新 | 随 N2-T5 / N3-T4 交付 | 路由表不新增入口，既有入口内容更新 |

### N5 — 收尾

| ID | 任务 | 交付物 | DoD |
|----|------|--------|-----|
| N5-T1 | CHANGELOG `[0.3.0]` | CHANGELOG.md | Keep a Changelog 格式；「明确不做（推迟至 v0.4）」清单；所有数字断言与实际产物核对 |
| N5-T2 | 版本与 tag | `pyproject.toml` 0.3.0；tag `v0.3.0` | ruff check + ruff format + pytest + evals 全绿后打 tag |
| N5-T3 | 推送远端 | push main + tag | 远端校验（`git ls-remote`） |

---

## 5. 整体验收标准（v0.3 DoD）

1. **嵌套可用且受控**：`:::card` 内嵌 note / quote / callout（含混排与嵌套属性如 `type="warning"`）从 parse → render 到 `lint_html` + `lint_gzh` 全绿；card 嵌 card、note 内嵌标记、深度 3 被 parser 与 lint 双双拒绝（`invalid_nesting`）。
2. **往返无损**：嵌套 AST `serialize → parse` 深度等价；扁平标记的解析与序列化输出与 v0.2 逐字节一致。
3. **向后兼容**：v0.2 全部 fixtures / examples / checkpoint 不改一字仍可被 v0.3 代码加载；既有 324 测试除「嵌套语义收窄」的定向改写外零回归。
4. **数据可查可留档**：`article_stats(date)` 四路径 mock 全测；`python scripts/stats.py` 产出终端摘要 + `outputs/stats/` 回执 JSON；61500/61501 语义化报错。
5. **规则可迭代**：humanize 9 维生效，新负例语料被检出、既有好文零误伤；evals 全绿且回归非零退出。
6. **工程基线**：ruff check + ruff format + pytest（3.11/3.13 矩阵）+ evals 全绿；CI 零网络依赖不破坏。

---

## 6. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 解析器重写引入回归（v0.1 起首次动 parser 主循环） | 全链路地基动摇 | 扁平输入逐字节一致的 DoD；既有 parser / lint / 快照测试零回归为硬门槛；嵌套范围保守（仅 card、深度 2） |
| lint 与 parser 行为漂移 | 同一输入两处判定不一致 | 共享 registry `allowed_children` + 深度常量（单一事实源）；防漂移测试断言两者报同类型错误 |
| 嵌套渲染触碰 gzh 白名单（新标签 / 新 CSS 属性） | 渲染门误杀 | 子组件复用既有发射器与样式键；间距用已白名单属性表达；5 主题 × 嵌套样例进渲染 eval |
| datacube 仅认证号可用、无法真实验证 | N2 只能 mock | mock 优先（同 v0.1 M5 / v0.2 N2 策略）；真实号验证步骤写入 verification-checklist 增补 |
| datacube 约束多（1 天跨度 / ≤ 昨日 / 30 天窗口 / 2025-11-01 起） | 使用方易踩坑 | CLI 客户端预检 + 语义化报错；约束在 publishing SKILL.md 与 checklist 双落档 |
| humanize 新维度误伤好文 | 好文 eval 跌破 90 | 新维度默认 warning 级 + 低 penalty；短文跳过保护（句数阈值）；校准只动 limits 不动 pass_score |
| 规则版本升级破坏旧 rules YAML / fixtures | 向后兼容破窗 | 模型新字段全部带默认值；v0.2 fixtures 不变仍合法作为 N3-T2 DoD |

---

## 7. v0.3 明确不做（Out of Scope，推迟至 v0.4）

- **任意深度嵌套与通用容器**（所有组件可嵌套 / card 嵌 card / 深度 > 2）：v0.3 受控嵌套先行验证，视真实使用反馈再放开。
- **datacube 其余接口**（用户 / 图文累计、接口分析、消息分析、留言分析等）：仅封装 getarticletotaldetail。
- **定时发布、群发标签 filter 细化**：延续 v0.2 决策，发布仍是显式人工动作。
- **LLM 辅助 humanize 判分**：违背 H8「Judge 永远是代码」，永久不做。
- **Web UI / 复杂 CLI 交互、性能优化、并发、缓存、i18n**：延续 v0.1/v0.2 决策。

---

## 8. 执行节奏建议

- **串行主线**：N1 → N4 → N5。
- **并行支线**：N2（数据统计）、N3（humanize v2）互不依赖，可与 N1 交替推进。
- **每里程碑出口**：DoD 全部勾选 + ruff / pytest 绿色，再进入下一里程碑。
- **N5 收尾后**：打 tag `v0.3.0`，推送远端 main，开启 v0.4 backlog（任意深度嵌套评估、datacube 接口族、规则集持续校准）。
