# wechat-content-skill — v0.4 排期计划

> **依据：** [v0.3-implementation-plan.md](v0.3-implementation-plan.md) 第 7 节「明确不做」与第 8 节收尾承诺（任意深度嵌套评估、datacube 接口族、规则集持续校准）+ [CHANGELOG.md](CHANGELOG.md) `[0.3.0]`「明确不做（推迟至 v0.4）」
> **目标版本：** v0.4（范围待观察期信号确认；若检查点全部 No-Go，降级为 **0.3.1 校准版**，不强行凑 0.4.0）
> **状态：** Draft —— 2026-09-28 Go/No-Go 检查点后升级为 Approved，并按 [v0.3-implementation-plan.md](v0.3-implementation-plan.md) 第 4 节的粒度细化任务分解
> **排期基准日：** 2026-09-13（v0.3.0 发布日）。若观察期实际启动日不同，下列日期整体平移、间隔不变。

---

## 1. v0.4 与 v0.1–v0.3 排期的本质差异

v0.1–v0.3 的范围在立项时即可锁定（技术纵深明确：发布 → 主题与框架 → 嵌套与数据回流）；v0.4 的主要候选的启动前提是 **v0.3 的真实使用信号**——嵌套深度 2 是否够用、stats 值不值得自动化、humanize 9 维有没有误报漏报。无信号就预做，等于为想象中的需求付维护成本。

因此本文档是**排期计划而非实施计划**：先观察、后决策、再细化。排期原则四条：

1. **信号先行**：无信号的候选项不预做（CHANGELOG 每版的「推迟理由」就是观察指标）。
2. **恒定项不等**：humanize 规则持续校准 + evals 语料补充是质量基线维护，不依赖新信号，恒定进 v0.4。
3. **范围纪律**：datacube 每版最多封装 2 个接口；嵌套扩容只走 registry 受控契约（扩 `allowed_children` 组合），不开放任意深度与任意互嵌。
4. **版本诚实**：检查点若全部 No-Go，本版降级为 0.3.1（仅校准 + 语料），或顺延检查点——不发没有内容支撑的版本。

---

## 2. 候选项全景评估

| # | 候选项 | 出处 | 价值假设 | Go 标准（观察期信号） | 成本 | 优先级 |
|---|--------|------|----------|----------------------|------|--------|
| 1 | **嵌套表达力扩容**（深度 > 2 或有限互嵌，如 callout 内嵌 note） | v0.3 计划 §7「任意深度嵌套与通用容器」 | 深度 2 / 现有组合不够真实表达 | 真实写作中出现被 `invalid_nesting` 拒绝的**合理**表达诉求 ≥ 2 例（记录于 §3 记录表） | 中：parser 栈已就绪，主要在 lint / 渲染 / eval 扩面 | P1（信号触发） |
| 2 | **发布数据自动化**（定期统计 + 周报落档） | CHANGELOG `[0.3.0]`「定时自动统计 / 数据看板」 | 手动 `stats.py` 高频重复，值得自动化 | 观察期内有回执的天数 ≥ 6 天，或用户明确点名自动化诉求 | 低-中：复用 `DatacubeService`，形态锁死见 §5 | P1（信号触发） |
| 3 | **humanize 语言风格维度**（如口语化程度） | CHANGELOG `[0.3.0]`「新语言风格维度」 | 9 维漏检某一类 AI 味（风格层面） | 记录表中的漏报 ≥ 3 例且指向**同一**风格缺口 | 中：确定性可判的新维度才进门禁（H8） | P1（信号触发） |
| 4 | **datacube 接口精选**（如 `getusercumulate` 用户累计、留言分析） | v0.3 计划 §7/§8「datacube 其余接口」 | 运营分析需要文章之外的数据（粉丝 / 留言） | 用户点名具体数据需求（不靠自动信号，靠显式需求） | 低-中：完全复刻 v0.3 N2 模式 | P2（需求触发） |
| 5 | **humanize 规则持续校准 + evals 语料补充** | v0.3 计划 §8「规则集持续校准」 | 观察期误报 / 漏报直接转化为词表、阈值、负例语料 | 无条件——观察期数据即输入 | 低：微调 + 补语料 | **P0（恒定）** |

## 3. 观察期设计（2026-09-14 → 2026-09-27，2 周）

**样本目标：≥ 7 篇真实文章**（订阅号每天 1 次群发额度，2 周理论上限 14 篇，7 为保守下限；不足时检查点顺延而非降低标准）。

**自动信号**（产物天然留档，检查点跑命令即可，无需额外开发）：

```bash
# 嵌套真实使用率：出现 component_N.M 子组件的 run 数（分母 = outputs/ 下 run 总数，排除 stats/）
grep -rlE 'component_[0-9]+\.[0-9]+' outputs/ --include=content_package.json 2>/dev/null | wc -l
find outputs -maxdepth 1 -mindepth 1 -type d ! -name stats 2>/dev/null | wc -l

# stats 使用频率：有回执的天数（幂等覆盖，查询次数 ≥ 回执天数）
find outputs/stats -maxdepth 1 -name '*.json' 2>/dev/null | wc -l
```

**人工信号**（随手追加到下表，空表本身也是有效信号——说明 9 维够用 / 手动 stats 够用）：

| 日期 | 类型（误报 / 漏报 / 嵌套受限 / 数据诉求） | 原文片段或描述 | 期望行为 |
|------|------------------------------------------|----------------|----------|
|      |                                          |                |          |

**观察期出口（DoD）**：

1. 样本量：`outputs/` 下真实文章 run ≥ 7（`find outputs -maxdepth 1 -mindepth 1 -type d ! -name stats | wc -l`，排除临时构造）。
2. 记录表已过目：每篇真实文章的 humanize 结果复核一遍，误报 / 漏报 / 嵌套受限随手入表（空表是有效负信号，如实保留，不追求数量）。
3. 自动信号可复核：§3 三条命令在检查点直接可跑、结果可复现（2026-09-13 已实测通过）。
4. 行为零改动：观察期区间内不出现 v0.3 行为面提交（`git log` 区间内无 `renderer/` / `skills/` / `integrations/` 变更）。

## 4. 排期时间线

| 阶段 | 日期（基准 2026-09-13） | 内容与出口条件 |
|------|------------------------|----------------|
| 观察期 | 09-14（周一）→ 09-27（周日） | 真实使用 v0.3 产出 ≥ 7 篇文章；记录表填写；**不改 v0.3 行为面**（边观察边改会让信号失真） |
| Go/No-Go 检查点 | 09-28（周一），约 30 分钟 | 跑 §3 命令 + 过 §2 各 Go 标准 → 确定范围与版本号（0.4.0 / 0.3.1）→ 本文档升级 Approved + 任务分解 |
| 开发窗口 | 09-29 → 10-09 | 视范围浮动：仅 P0 校准 1–2 天；单个 P1 分支 3–4 天；两个 P1 分支 ≤ 7 天（参照 v0.2 用 1 天 / v0.3 用 2 天的节奏，上浮因含真实样本回填） |
| 收尾 | ≤ 10-09 | CHANGELOG → ruff + pytest + evals 全绿 → tag → 推送远端（对齐 v0.1–v0.3 惯例） |

检查点可**提前**：若用户在观察期内直接点名某些候选（如「要粉丝数据」），不等信号，立即定范围进入开发。

**检查点产出物（验收）**：

1. 本文档状态行 Draft → Approved，并记录版本定调（0.4.0 或 0.3.1）及各候选 Go/No-Go 结论与依据（信号数字）。
2. §5 各 Go 分支细化为 Nx-Ty 任务表，粒度对齐 [v0.3-implementation-plan.md](v0.3-implementation-plan.md) 第 4 节，每任务指定验证命令。
3. 开发窗口日期按实际启动日校准（间隔不变）。

## 5. Go 分支的范围边界与验收标准

> 验收标准在检查点前即锁定：开发只负责实现与证明，不临时改标准。完整任务分解（Nx-Ty 表）在检查点后按实际 Go 项细化。

**N1 嵌套扩容**（若信号 1 Go）

范围边界：只扩 `allowed_children` 组合（候选：callout → note）；`MAX_COMPONENT_DEPTH` 维持 2 或升 3，**二选一**，不同时放开。

验收标准：

1. 新增组合解析成功且 `node_id` 编号规则延续（深度升 3 时为 `component_N.M.L`）；registry 唯一事实源，parser 与 component_lint 判定逐一同构。
2. 超深与非法组合被 parser 与 lint 双双拒绝，错误码 `invalid_nesting` 归属父组件，阈值由测试锁定。
3. 新增组合在全部 5 主题下渲染通过，不引入白名单外新 HTML 标签 / 属性（白名单变更须 CHANGELOG 显式记录）。
4. render suite 用例 10 → ≥ 12（render_nested.md 覆盖新组合）。
5. 既有 27 evals 零回归；嵌套相关新增测试 ≥ 8 个。
6. card.md 嵌套契约表更新，「文档即测试」守卫全绿。

**N2 stats 自动化**（若信号 2 Go）

范围边界：形态锁死为 **cron / 定期脚本 + Markdown 周报落档 `outputs/stats/`**——不引入常驻服务、不做 Web 看板、不进 pipeline 状态机（延续「查询型能力不进状态机」决策）。

验收标准：

1. 交付物 = 周报生成脚本 + cron 配置示例；无 daemon 进程；pipeline 状态机 diff 为零。
2. 周报幂等覆盖（同一周重复执行，周报内容不变）。
3. 周报聚合数字与逐日回执按 `stat_date` 逐字段加总相等（mock 锁定）。
4. mock 不触网，新增测试 ≥ 10 个。
5. 失败路径退出码非 0、既有回执不受污染。
6. verification-checklist §9 增补周报使用步骤。

**N3 humanize 风格维度**（若信号 3 Go）

范围边界：只收**确定性可判**维度（如口语化 = 语气词 / 网络用语词表 + 频率统计）；rules v2 → v3 旧字段不删不改；情感基调这类主观维度最多进「参考提示」，**永不进门禁**（H8：Judge 永远是代码）。

验收标准：

1. rules.yaml version 2 → 3；v2 配置在 v3 加载器下语义不变（向后兼容测试）。
2. 新维度纯函数实现，阈值 / 扣分 / 豁免门限全部显式配置于 YAML。
3. 零误伤：good_01 / good_02 得分 ≥ 90 不变。
4. 负例语料 ≥ 2 份进 evals 且新维度 warning 触发。
5. humanize suite 6 → ≥ 8；每个新维度 ≥ 1 个锁定测试（阈值锁定测试 14 → 14 + 新维度数）；既有 27 evals 零回归。
6. 维度数字（9 → N）在 rules.yaml 注释、SKILL.md、README 三处同步零偏差。

**N4 datacube 精选**（若需求点名）

范围边界：**每版 ≤ 2 个接口**（建议 `getusercumulate` + 留言分析二选一）；完全复刻 v0.3 N2 模式（frozen dataclass + Service + 可选 CLI 子命令）；仅认证号可用等约束同步落档 verification-checklist。

验收标准：

1. 接口数 ≤ 2。
2. 每接口 mock 四路径（成功 / 空数据 / 微信错误码 / 网络异常），每接口 ≥ 8 测试。
3. 字段类型错误 / 缺失抛 ValidationError。
4. CLI 退出码语义对齐 stats.py（0 / 1 / 2）。
5. 回执幂等落档。
6. verification-checklist 落档接口名与数据约束。

**N5 规则校准 + 语料**（恒定，P0）

范围边界：观察期误报 → 收紧词表 / 阈值；漏报 → 扩黑名单 / 成对句式 + 补负例语料；校准只动 `limits` 不动 `pass_score`（v0.3 决策延续）。

验收标准：

1. `pass_score` 前后 diff 为零。
2. 每条误报 / 漏报闭环：变更 + ≥ 1 个回归测试锁定，可追溯到 §3 记录表。
3. good_01 / good_02 得分 ≥ 90。
4. evals 27 → ≥ 28（语料 16 → ≥ 17）。
5. 校准记录逐条落 CHANGELOG。

**N6 文档 + 收尾**（恒定）

范围边界：受影响子技能文档与 README / 根 SKILL.md 数字核验更新；CHANGELOG「明确不做」滚动下一版。

验收标准：

1. 文档零偏差（全部数字以实际产物核验）。
2. CHANGELOG 遵循 Keep a Changelog 格式 + 「明确不做」滚动清单。
3. pyproject 版本 = 定调版本；代码无版本号断言。
4. 工程基线全绿 + tag 推送校验（版本级按 §6 全量执行）。

## 6. 版本级验收标准（无论范围如何均适用）

**基线（v0.3.0）**：pytest 383 passed / evals 27 用例（humanize 6 · content_gate 4 · render 10 · framework 7）/ 语料 16 份 / 阈值锁定测试 14 个 / humanize 9 维。

1. **范围纪律**：实际交付范围 ⊆ 检查点确认范围；三条硬约束 diff 可查——datacube 接口 ≤ 2、嵌套深度与互嵌不同时放开、无常驻服务进程。
2. **零回归门槛**：既有 27 evals 全过、既有语料判定不变；pytest 总数 ≥ 383 且失败数 = 0；ruff 0 错误。
3. **观察闭环**：§3 记录表每条记录有去向——已修复（对应测试）/ 已封装（对应接口）/ 明确不做（记录再评估条件），三者必有其一。
4. **版本诚实**：检查点全 No-Go 时版本号为 0.3.1 且 CHANGELOG 如实标注「校准版」；0.4.0 必须有 ≥ 1 个 Go 分支完整落地。
5. **文档零偏差**：README / 根 SKILL.md / verification-checklist / 子技能文档中全部数字（测试数 / 用例数 / 维度数 / 语料数 / 接口数）逐一以实际产物核验，无凭空数字。
6. **发布完整**：pyproject 版本 = 定调版本；tag 与远端 main 指向同一 commit（`git ls-remote` 校验）；CHANGELOG 含「明确不做」滚动清单。

## 7. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 观察期无真实使用，信号为零 | Go/No-Go 全 No-Go，v0.4 无内容 | 接受降级 0.3.1 或顺延检查点；不发空版本（版本诚实原则） |
| 嵌套扩容触碰 gzh 白名单 / lint 复杂度陡增 | 渲染门误杀、防漂移成本上升 | 仍走 registry 受控契约（单一事实源）；扩面进渲染 eval；深度与互嵌不同时放开 |
| 风格维度滑向主观判定 | 违背 H8，Judge 不可复现 | 只有词表 / 统计型维度进门禁；主观维度最多做参考提示 |
| 自动化形态漂移（常驻服务 / 看板） | 违背「Agent + scripts」入口决策，维护面失控 | 形态在本文档锁死：cron + 脚本 + Markdown 回执 |
| datacube 接口族范围蔓延 | 一版塞全家桶，测试与文档负担失控 | 每版 ≤ 2 接口，按点名封装 |
| 校准误伤既有基线 | 好文 eval 跌破 90 | 只动 limits 不动 pass_score；既有 27 用例零回归为硬门槛 |

## 8. 明确不启动（含再评估条件）

- **Web UI / 复杂 CLI 交互**：再评估条件 = 出现非 Agent 使用者，或脚本入口被证明不敷使用。
- **性能优化 / 并发 / 缓存**：再评估条件 = 出现可感知的性能痛点（当前 383 测试 + evals 无任何性能信号）。
- **i18n**：公众号场景内容恒为中文，不做。
- **定时发布 / 群发标签 filter 细化**：群发不可撤回，显式人工动作是安全决策（v0.2 起连续三版确认）；再评估条件 = 用户明确要求。
- **LLM 辅助 humanize 判分**：违背 H8「Judge 永远是代码」，**永久不做**。

## 9. 执行节奏建议

- 观察期零行为面改动（只记录），保证信号纯净。
- 检查点的唯一产物：本文档 Draft → Approved（§5 验收标准细化为任务表）+ 版本定调。
- 开发节奏参照 v0.3：主线串行、独立支线（如 N4 与 N1/N2/N3）并行、每里程碑 DoD 出口后才进下一个。
- 收尾后按惯例更新 README 计划链接（v0.4 落地记录），并滚动生成下一版「明确不做」清单。
