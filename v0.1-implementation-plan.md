# wechat-content-skill — v0.1 实施计划

> **依据：** [blueprint.md](blueprint.md)（v1 架构基线，第二十章 v0.1 落地范围）
> **目标版本：** v0.1（repository skeleton + 可跑通的 happy path）
> **状态：** Draft

---

## 1. v0.1 唯一目标

落成 `wechat-content-skill` 的 repository skeleton，并让一条真实链路端到端跑通：

> **一句话 → 研究 → 写作 → 排版 → 校验 → 微信草稿**

衡量标准不是"功能多"，而是：**每个阶段都有确定性交付物（Artifact），每个 Artifact 都有 Schema，每次失败都能局部修复，每次中断都能 Resume。**

### 交付物总览（对应蓝图第二十章 10 项）

| # | 蓝图要求 | v0.1 交付物 | 里程碑 |
|---|----------|------------|--------|
| 1 | SKILL.md 薄编排入口 | 根 `SKILL.md`（≤150 行） | M2 |
| 2 | 6 个 Sub-Skill 目录 | research / content / visual / native / layout / publishing | M2–M5 |
| 3 | Artifact schema | `core/artifacts/` 8 个 Pydantic 模型 | M1 |
| 4 | Orchestrator（State/Checkpoint/Resume） | `core/state/` + `core/workflow/` | M1, M6 |
| 5 | AST / Renderer | `renderer/`（ast + default 主题 + html） | M3 |
| 6 | component_lint | `validators/component/` | M4 |
| 7 | gzh_validator | `validators/wechat/` + `validators/html/` | M4 |
| 8 | WeChat API Facade | `integrations/wechat/`（client/auth/media/draft/publish） | M5 |
| 9 | 测试 fixtures | `tests/fixtures/` 全链路示例 Artifact | M6 |
| 10 | Happy path | 离线 e2e + 真实环境验证清单 | M6 |

---

## 2. 关键技术决策

| 决策点 | 选择 | 理由（蓝图依据） |
|--------|------|------------------|
| 语言/包管理 | Python 3.11+，`pyproject.toml` | 蓝图十五章 repo 结构即 Python 工程 |
| Artifact 定义 | Pydantic v2 + JSON Schema 导出 | Artifact-native：可校验、可序列化、可缓存 |
| 状态与 Checkpoint | 文件系统 JSON（`outputs/<run_id>/`） | 无外部依赖，天然支持 Resume（蓝图十一章） |
| 语义标记 | 有限集：`:::note` / `:::quote` / `:::callout` / `:::card` | 控制解析复杂度（蓝图六章） |
| 主题 | 仅 `default` 一套（YAML 驱动） | Theme Engine 架构就位即可，不追主题数量（蓝图七章） |
| HTML 生成 | AST → Renderer 确定性生成，**LLM 不直接产出 HTML** | 蓝图八章硬性要求 |
| 微信集成 | `httpx` 直连官方 API，测试全部 mock | Facade 模式（蓝图十三章）；无重型 SDK 依赖 |
| 外部能力 | LLM / 搜索 / 图片均为 Provider 接口 + 环境变量切换，各 1 个默认实现 | Provider-agnostic（蓝图十八章⑤） |
| "6 个 Sub-Skill"取舍 | 取 research / content / visual / native / layout / publishing；**quality 逻辑归入 `validators/`，`skills/quality/` 推迟至 v0.2** | 蓝图二十章列 7 项但要求 6 个；Quality Gate 本质是确定性工具，符合 Deterministic-first |
| 测试框架 | pytest + ruff + GitHub Actions CI | 工程基线 |

---

## 3. 里程碑与依赖关系

```text
M0 工程骨架
   ↓
M1 Artifact 基础设施（schema + state + checkpoint）
   ↓
M2 内容生产 Skills ──→ M3 语义层与渲染 ──→ M4 质量门与修复 ──┐
   (research/content/visual)   (native/layout/renderer)   (validators/repair)  ↓
   └──────────────→ M5 集成层与微信 Facade ──────────────────────→ M6 端到端联调与收尾
```

- M1 是全局地基：所有后续里程碑消费 Artifact schema。
- M5 只依赖 M1，可与 M2–M4 **并行开发**（独立集成微信 API）。
- M6 必须最后：串联一切并验证 happy path。

---

## 4. 任务分解

> 每个任务标注：交付物、验收标准（DoD）、前置依赖。任务编号 `Mx-Ty`。

### M0 — 工程骨架

| ID | 任务 | 交付物 | DoD | 依赖 |
|----|------|--------|-----|------|
| M0-T1 | 按蓝图十五章初始化完整目录树 | 全部目录 + `.gitkeep` | `tree` 结构与蓝图一致 | — |
| M0-T2 | `pyproject.toml` + 依赖锁定 | pydantic / pyyaml / httpx / pytest / ruff | `pip install -e .[dev]` 成功 | T1 |
| M0-T3 | LICENSE（MIT）+ README 快速上手占位 | LICENSE、README 更新 | — | — |
| M0-T4 | CI 流水线 | `.github/workflows/ci.yml` | lint + test 在空测试集下通过 | T2 |
| M0-T5 | `.gitignore`（outputs/、secret、缓存） | .gitignore | `git status` 不含运行产物 | T1 |

### M1 — Artifact 基础设施

| ID | 任务 | 交付物 | DoD | 依赖 |
|----|------|--------|-----|------|
| M1-T1 | 8 个 Artifact 模型：ResearchResult / ContentBrief / ArticleDraft / VisualPlan / ContentPackage / WechatDocument / ValidationReport / PublishResult | `core/artifacts/` + `schemas/`（JSON Schema 导出） | 字段与蓝图二、五、九、十章示例对齐；schema 导出脚本可运行 | M0 |
| M1-T2 | WorkflowState 状态机：蓝图十一章 13 个状态 + 合法转换表 + 转换守卫 | `core/state/` | 非法转换（如 INIT→RENDERING）抛异常并有单测 | M0 |
| M1-T3 | Checkpoint 机制：每状态落盘 `outputs/<run_id>/checkpoint.json` + 阶段产物 | `core/state/checkpoint.py` | 任意阶段 kill 进程后可读到完整 checkpoint | T2 |
| M1-T4 | Orchestrator 骨架：run / resume / 状态推进 | `core/workflow/orchestrator.py` | resume 从最近 checkpoint 继续，不重跑已完成阶段 | T3 |
| M1-T5 | 单测：schema 往返序列化、非法状态转换、resume 恢复 | `tests/unit/` | 覆盖上述三条路径 | T1–T4 |

### M2 — 内容生产 Skills（Agent 文档 + 确定性工具）

| ID | 任务 | 交付物 | DoD | 依赖 |
|----|------|--------|-----|------|
| M2-T1 | 根 `SKILL.md`：严格按蓝图十六章模板（Purpose / Workflow / Rules / Routing） | SKILL.md | ≤150 行；Routing 覆盖 6 个 Sub-Skill | M1 |
| M2-T2 | research skill：搜索→阅读→提取→交叉验证→结构化，产出 ResearchResult | `skills/research/SKILL.md` + `references/` | 输出能通过 M1-T1 的 ResearchResult 校验 | M2-T1 |
| M2-T3 | content skill + **2 个**写作框架（tutorial / news-analysis） | `skills/content/SKILL.md` + `frameworks/` + `writing/` | 框架可按选题路由，新增框架无需改 Orchestrator | M2-T1 |
| M2-T4 | humanize 确定性检测器：AI 味黑名单短语 + 句长/段落/重复指标 + `humanize_score` | `skills/content/humanize/`（代码）+ `references/humanize.md` | 对正/反例文本打分行为符合预期（蓝图四章） | M1 |
| M2-T5 | visual skill：产出结构化 VisualPlan（cover / images / diagrams） | `skills/visual/SKILL.md` + `prompts/` | 输出通过 VisualPlan schema 校验（蓝图五章） | M2-T1 |
| M2-T6 | references 首版：writing-guide / humanize / wechat-rules | `references/` | — | — |
| M2-T7 | humanize 检测器单测 | `tests/unit/` | 黑名单命中、评分区间、pass/fail 阈值 | M2-T4 |

### M3 — 语义层 → AST → 渲染

| ID | 任务 | 交付物 | DoD | 依赖 |
|----|------|--------|-----|------|
| M3-T1 | native skill：语义标记规范（4 种标记 + 属性约定） | `skills/native/SKILL.md` + `components/` | 标记集合有明确 schema 定义（蓝图六章） | M1 |
| M3-T2 | Article AST 节点定义（heading / paragraph / quote / note / callout / card / image / code / list / hr） | `renderer/ast/` | 每类节点为 Pydantic 模型（蓝图八章） | M1 |
| M3-T3 | 解析器：Markdown + `:::` 语义标记 → ContentAST | `renderer/ast/parser.py` | 对含 4 种标记的样例文档解析正确；round-trip 单测 | T1, T2 |
| M3-T4 | default 主题包 | `renderer/themes/default/`（theme / typography / components.yaml） | 配置结构与蓝图七章示例一致 | — |
| M3-T5 | HTML Renderer：ContentAST → `wechat.html`（inline CSS、主题驱动） | `renderer/html/` | 输出零 `<style>` 外链、全部 inline style；快照测试 | T3, T4 |
| M3-T6 | layout skill（Agent 侧：主题选择与排版规则） | `skills/layout/SKILL.md` | 指向 renderer/themes 的使用说明 | T4 |
| M3-T7 | 命令行工具：render / preview | `scripts/render.py`、`scripts/preview.py` | 从 ContentPackage 一键产出 HTML 并本地预览 | T5 |

### M4 — 质量门与 Repair Loop

| ID | 任务 | 交付物 | DoD | 依赖 |
|----|------|--------|-----|------|
| M4-T1 | component_lint：unknown component / missing required prop / invalid nesting / unsupported attribute / theme component missing | `validators/component/` | 蓝图 9.2 全部 5 类错误均有检出单测 | M3-T2 |
| M4-T2 | gzh_validator：inline CSS 白名单、图片 URL/尺寸、外部资源、HTML size、空节点、坏图 | `validators/wechat/` + `validators/html/` | 蓝图 9.3 全部 9 项检查可执行且各有用例 | M3-T5 |
| M4-T3 | Content QA v1：结构完整性、字数、AI 味复检、引用 ID 一致性 | `validators/content/` | 引用不存在的 source_id 能被拦截 | M2-T4 |
| M4-T4 | ErrorReport schema + Repair Loop：按 node 定位定向修复，上限 3 轮 | `core/workflow/repair.py` | 蓝图十章示例的 `unsupported_css@component_17` 场景只改该节点即复检通过 | T1, T2 |
| M4-T5 | 命令行工具：validate / lint | `scripts/validate.py`、`scripts/lint.py` | 对坏样例返回非零退出码 + 结构化 ErrorReport | T1–T3 |

### M5 — 集成层与微信 Facade（可与 M2–M4 并行）

| ID | 任务 | 交付物 | DoD | 依赖 |
|----|------|--------|-----|------|
| M5-T1 | LLM Provider 接口 + 1 个默认实现（环境变量选择） | `integrations/llm/` | 切换供应商只需改环境变量（蓝图十八章⑤） | M1 |
| M5-T2 | Search Provider 接口 + 默认实现 + 降级（失败→使用既有 sources 继续） | `integrations/search/` | 断网/超时用例走降级分支（蓝图十二章） | M1 |
| M5-T3 | Image Provider 接口 + 默认实现 + 降级（生成失败→图片搜索→占位图） | `integrations/image/` | 三级降级链路有集成测试 | M1 |
| M5-T4 | 微信 client + auth：access_token 获取、缓存与过期刷新 | `integrations/wechat/client.py`、`auth.py` | token 过期自动刷新有单测（mock 时钟） | M0 |
| M5-T5 | 媒体上传：封面与正文图上传换取微信 URL | `integrations/wechat/media.py` | mock 上传返回 media_id 并写入 ContentPackage | T4 |
| M5-T6 | DraftService：`publisher.create_draft(article)` 高层封装 | `integrations/wechat/draft.py`、`publish.py` | 上层零感知 access_token/media_id 等细节（蓝图十三章） | T5 |
| M5-T7 | 多账号配置规范（YAML 只引用环境变量，不落 secret） | `accounts/accounts.example.yaml` | 配置文件中 grep 不到任何真实密钥（蓝图十四章） | T4 |
| M5-T8 | publishing skill + 降级手册（API 挂→导出 HTML→本地 artifact→人工发布） | `skills/publishing/SKILL.md` + `references/` | API 失败时流程仍以本地 HTML 收尾（蓝图十二章） | T6 |
| M5-T9 | 微信集成测试（全部 mock，不触网） | `tests/integration/` | CI 中无网络依赖 | T4–T6 |

### M6 — 端到端联调与收尾

| ID | 任务 | 交付物 | DoD | 依赖 |
|----|------|--------|-----|------|
| M6-T1 | Orchestrator 串联完整 13 状态流程（Intent→…→DRAFT_CREATED） | `core/workflow/` 完整实现 | 蓝图十一章状态序列全覆盖 | M1–M5 |
| M6-T2 | 全链路测试 fixtures：每个 Artifact 各 1 份合法示例 | `tests/fixtures/` | fixtures 即文档：外部贡献者可照此理解 Artifact Flow | M1 |
| M6-T3 | 离线 e2e 测试（mock LLM/搜索/图片/微信） | `tests/integration/test_e2e.py` | 一句话输入 → 校验通过的 WechatDocument + mock 草稿创建 | 全部 |
| M6-T4 | 真实环境 happy path 验证清单（人工执行） | `docs/verification-checklist.md` | 在测试号或正式号完成一次真实「一句话→草稿箱」 | M6-T3 |
| M6-T5 | 首个可复现示例 | `examples/tutorial/` | 示例含输入、全部中间 Artifact、最终 HTML | M6-T3 |
| M6-T6 | README 快速上手 + v0.1 发布说明 | README.md、CHANGELOG | 新用户按文档 10 分钟内跑通离线 e2e | M6-T3 |

---

## 5. 整体验收标准（v0.1 DoD）

1. **Happy path 成立**：按根 SKILL.md 的工作流执行「一句话」需求，产出校验通过的 `wechat.html`，并成功创建微信草稿（真实或 mock 环境）。
2. **Artifact-native**：阶段间只传递结构化 Artifact，每个 Artifact 有 JSON Schema，可单独检视、复用、缓存。
3. **可恢复**：任意阶段中断后 `resume` 从最近 checkpoint 继续，不重跑已完成阶段。
4. **门禁有效**：Quality Gate（Content QA + component_lint + gzh_validator）全绿才允许进入 UPLOADING；失败走 Targeted Repair（≤3 轮）而非整篇重写。
5. **优雅降级**：搜索、图片生成、微信 API 任一失败时，流程均有兜底出口；微信 API 不可用时以本地 HTML artifact 收尾——**发布不是唯一出口**。
6. **安全合规**：仓库与配置文件中不存在任何真实 secret；账号配置只引用环境变量。
7. **工程基线**：CI（ruff + pytest 全量）绿色；gzh_validator 至少覆盖蓝图 9.3 的 9 项检查并各有测试用例。

---

## 6. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 微信测试号无草稿 API 权限 | M5 无法真实验证 | mock 优先（M5-T9）；HTML 导出兜底本就是架构要求（蓝图十二章） |
| 语义标记嵌套解析复杂度超预期 | M3 延期 | v0.1 限定 4 种标记、不支持标记嵌套；嵌套需求进 v0.2 |
| LLM 输出不符合 Artifact schema | 全链路不稳定 | Pydantic 严格校验 + 失败即拒 + 单点重试；禁止静默降级为自然语言传递 |
| humanize 规则误报/漏报 | 内容质量争议 | 规则外置可配置、阈值可调；规则集迭代与 evals 推迟 v0.2 |
| 范围蔓延 | v0.1 无法收敛 | 严格遵守第 7 节"明确不做"；新想法一律进 v0.2 backlog |
| 微信 inline CSS 白名单维护成本 | gzh_validator 误杀 | 白名单外置于 `validators/wechat/rules/`，可随微信规则更新而不改代码 |

---

## 7. v0.1 明确不做（Out of Scope）

- **正式发布 / 群发 / 定时 / 监控**：仅到「草稿箱」为止（蓝图十章 `*` 标注能力全部推迟）。
- **多主题**：仅 `default` 主题；editorial / minimal / tech / magazine 进 v0.2，但 Theme Engine 接口需为扩展预留。
- **写作框架**：仅 tutorial、news-analysis 两个；其余（opinion / case-study / listicle / deep-dive / narrative）进 v0.2。
- **`skills/quality/` 目录**：校验逻辑全部落在 `validators/`（确定性代码），Agent 侧质量规则文档进 v0.2。
- **evals / SEO（references/seo.md）/ content-policy 高级规则**：进 v0.2。
- **Web UI、复杂 CLI 交互**：入口只有 Agent（SKILL.md）+ `scripts/`。
- **性能优化、并发、缓存、i18n**：一概不做。

---

## 8. 执行节奏建议

- **串行主线**：M0 → M1 → M2 → M3 → M4 → M6。
- **并行支线**：M5（微信 Facade 与集成层）只依赖 M1，可由第二人/第二会话独立推进，与 M2–M4 并行。
- **每里程碑出口**：DoD 全部勾选 + CI 绿色 + 中间产物合入 `main`，再进入下一里程碑。
- **M6 收尾后**：打 tag `v0.1.0`，撰写 release notes，开启 v0.2 backlog（多主题、更多框架、正式发布、evals）。
