# wechat-content-skill — v0.2 实施计划

> **依据：** [blueprint.md](blueprint.md)（v1 架构基线）+ [v0.1-implementation-plan.md](v0.1-implementation-plan.md) 第 7 节 v0.2 backlog
> **目标版本：** v0.2（多主题、更多写作框架、正式发布、evals）
> **状态：** Approved

---

## 1. v0.2 唯一目标

在 v0.1「一句话 → 草稿箱」happy path 之上，补齐蓝图承诺的**表达面**（多主题 × 多框架）与**交付面**（正式发布），并让质量本身**可度量**（evals）。

衡量标准：**同一份 ContentPackage 换主题 / 换框架零代码改动；草稿可经 Freepublish 正式发布且全链路 mock 可测；humanize 与渲染质量有确定性 eval 报告，回归可比较。**

### backlog 对照（v0.1 计划第 7 节 → v0.2 交付物）

| # | v0.1 明确不做 | v0.2 交付物 | 里程碑 |
|---|--------------|------------|--------|
| 1 | 多主题（editorial / minimal / tech / magazine） | 4 套新主题 YAML，全部通过既有渲染门校验 | N1 |
| 2 | 更多写作框架（opinion / case-study / listicle / deep-dive / narrative） | 5 个框架定义 + 框架路由表更新 | N1 |
| 3 | 正式发布 / 群发 / 定时 / 监控 | FreepublishService（submit + 状态查询）+ Publisher.release() + 状态机打通 PUBLISHED | N2 |
| 4 | evals | 确定性 eval 套件（humanize 评分 + 全主题渲染门）+ CLI 汇总报告 | N3 |
| 5 | `skills/quality/` 目录 | Agent 侧质量规则 SKILL.md + rules/（与 validators/ 一一对应的说明层） | N4 |
| 6 | SEO（references/seo.md） | 公众号 SEO 参考（标题 / 摘要 / 搜一搜） | N4 |
| 7 | content-policy 高级规则 | references/content-policy.md（合规红线 + 敏感内容约束） | N4 |

---

## 2. 关键技术决策

| 决策点 | 选择 | 理由（蓝图 / v0.1 依据） |
|--------|------|------------------------|
| 主题实现 | 纯 YAML（theme / typography / components），零代码 | 主题引擎本就 YAGNI 零代码扩展（v0.1 计划 2）；CSS 只用渲染器已发射属性，天然通过 `ALLOWED_CSS_PROPS` 白名单 |
| 打包 | `pyproject.toml` package-data 由 `default/*.yaml` 改为 `*/​*.yaml` | 主题作为包数据分发；`load_theme` 运行时发现 |
| 发布 API | `cgi-bin/freepublish/submit` + `cgi-bin/freepublish/get`（微信正式发布接口） | 群发次数受限、发布接口为主流（蓝图十章 `*` 能力）；定时 / 群发标签通过 `filter` 参数表达 |
| PublishResult 扩展 | 新增 `status="published"`、`publish_id`、`article_url` 字段（全部带默认值） | Artifact 演进向后兼容：v0.1 fixtures 不变仍合法 |
| 发布触发 | pipeline 新增可选阶段（`DRAFT_CREATED` 入口，`auto_release=False` 默认关） | 状态机 `DRAFT_CREATED → PUBLISHED` v0.1 已预留；不发布仍以草稿箱终态 |
| evals 判分 | **全部确定性指标**（humanize score、三层门禁通过、字数口径），零 LLM 参与判分 | 硬约束 H8「Judge 永远是代码」；evals 可在 CI 离线运行 |
| 框架形态 | 纯 Markdown 定义（结构骨架 + 写作规则 + 字数预算 + 自审清单） | 与 v0.1 两个框架同构；新增框架不改 Orchestrator（蓝图三章） |
| 版本 | `pyproject.toml` 0.1.0 → 0.2.0；打 tag `v0.2.0` | 语义化版本：新增能力、无破坏性变更 |

---

## 3. 里程碑与依赖关系

```text
N1 表达面：多主题 + 多框架（纯资产，无代码风险）
   ↓（evals 消费 N1 的全部主题与框架）
N2 交付面：正式发布（模型 + Service + Pipeline，独立于 N1）
   ↓
N3 evals：确定性评估套件（依赖 N1 语料 + 渲染门）
   ↓
N4 文档面：skills/quality/ + seo + content-policy（纯文档）
   ↓
N5 收尾：CHANGELOG / README / SKILL.md / 版本号 / 全量验证 / tag
```

- N1 与 N2 无依赖，可并行。
- N3 依赖 N1（主题全集参与渲染 eval）。
- N5 必须最后：全量回归 + 版本标记。

---

## 4. 任务分解

> 任务编号 `Nx-Ty`；每任务标注交付物与验收标准（DoD）。

### N1 — 表达面：多主题与多框架

| ID | 任务 | 交付物 | DoD |
|----|------|--------|-----|
| N1-T1 | editorial 主题（编辑部风：衬线感、深色标题、大字引文） | `renderer/themes/editorial/`（3 YAML） | `load_theme("editorial")` 成功；全组件渲染 HTML 通过 `lint_html` + `lint_gzh` |
| N1-T2 | minimal 主题（极简：黑白灰、细分隔、轻背景） | `renderer/themes/minimal/` | 同上 |
| N1-T3 | tech 主题（科技感：等宽数字、蓝强调、代码块深色） | `renderer/themes/tech/` | 同上 |
| N1-T4 | magazine 主题（杂志风：高对比、粗标题、暖色卡片） | `renderer/themes/magazine/` | 同上 |
| N1-T5 | 主题清单测试 + 打包修正 | `tests/unit/test_theme_pack.py`（或并入既有 renderer 测试）；`pyproject.toml` package-data `*/*.yaml` | 5 主题全部 load + 渲染 + 门禁通过；`pip install -e .` 后运行时可发现 |
| N1-T6 | 5 个写作框架：opinion / case-study / listicle / deep-dive / narrative | `skills/content/frameworks/*.md`（各含结构骨架、写作规则、字数预算、自审清单、humanize 专项提示） | 结构与 v0.1 两框架同构；`ContentBrief.framework` description 更新为 7 框架全集 |
| N1-T7 | content skill 框架路由表更新 | `skills/content/SKILL.md` | 框架表覆盖 7 框架；移除「v0.2 待办」标注 |

### N2 — 交付面：正式发布

| ID | 任务 | 交付物 | DoD |
|----|------|--------|-----|
| N2-T1 | `PublishResult` 模型扩展 | `core/artifacts/models.py`：新增 `published` 状态、`publish_id`、`article_url` 字段 | v0.1 fixtures（无新字段）仍通过校验（向后兼容）；schema 重新导出 |
| N2-T2 | `FreepublishService` | `integrations/wechat/freepublish.py`：`submit(draft_media_id)` → `publish_id`；`status(publish_id)` → 结构化状态 | token 过期自动重试（复用 `call_with_token_retry`）；errcode 统一 `ProviderError` |
| N2-T3 | `WeChatPublisher.release()` | `integrations/wechat/publish.py`：`release(draft_id)` 组合 submit；失败返回 `PublishResult(status="degraded")`，绝不抛出 | API 不可用时降级出口成立（蓝图十二章）；成功返回 `published` + `publish_id` |
| N2-T4 | pipeline 可选发布阶段 | `core/workflow/pipeline.py`：`PipelineDeps.auto_release`（默认 False）+ `DRAFT_CREATED` 入口阶段 | 不开启 auto_release 时行为与 v0.1 完全一致（既有 e2e 不改即过）；开启后推进到 `PUBLISHED` |
| N2-T5 | 发布链路 mock 测试 | `tests/unit/test_freepublish.py` + 扩展 `tests/integration/test_wechat_publish_flow.py` | 成功 / errcode 失败 / HTTP 500 降级 / token 40001 重试 四条路径全覆盖，CI 零网络 |
| N2-T6 | publishing skill 文档更新 | `skills/publishing/SKILL.md` | 覆盖 release 工作流、群发次数约束、状态查询与降级手册 |

### N3 — evals：确定性评估套件

| ID | 任务 | 交付物 | DoD |
|----|------|--------|-----|
| N3-T1 | eval 用例语料 | `tests/evals/cases/*.md`（好文 / AI 味文 / 结构缺失文） | 每类 ≥2 例；不依赖网络与 LLM |
| N3-T2 | humanize eval | `tests/evals/test_humanize_eval.py` | 好文 `humanize_score ≥ 阈值` 且 `passed`；AI 味文被检出；报告写入 `outputs/evals/` |
| N3-T3 | 渲染 eval（全主题 × 全组件） | `tests/evals/test_render_eval.py` | 5 主题渲染同一 ContentPackage，HTML 全部通过 `lint_html` + `lint_gzh`；任一失败即 eval FAIL |
| N3-T4 | 框架 eval（fixture 走全链路） | `tests/evals/test_framework_eval.py` | 每框架至少 1 份 brief/draft 样例，渲染 + 三层门禁全绿 |
| N3-T5 | eval CLI 汇总 | `scripts/run_evals.py` | 一键跑全部 eval，输出 JSON 报告 + 非零退出码表示回归 |

### N4 — 文档面：质量规则与参考资料

| ID | 任务 | 交付物 | DoD |
|----|------|--------|-----|
| N4-T1 | `skills/quality/` 目录 | `SKILL.md` + `rules/`（content / component / html / wechat 四份，与 `validators/` 模块一一对应） | Agent 侧「何时信任哪个门」有唯一权威说明；根 SKILL.md 路由表补 quality 行 |
| N4-T2 | `references/seo.md` | 公众号 SEO 参考 | 标题 / 摘要 / 关键词 / 搜一搜收录四节；与 ContentBrief / digest 字段对应 |
| N4-T3 | `references/content-policy.md` | 内容合规红线 | 敏感内容约束 + 引用规范 + 免责边界；被 content / quality skill 引用 |

### N5 — 收尾

| ID | 任务 | 交付物 | DoD |
|----|------|--------|-----|
| N5-T1 | CHANGELOG `[0.2.0]` | CHANGELOG.md | Keep a Changelog 格式；「明确不做（推迟至 v0.3）」清单 |
| N5-T2 | README / 根 SKILL.md 更新 | README.md、SKILL.md | 主题清单、框架清单、发布 CLI、evals 用法全部反映 v0.2 能力 |
| N5-T3 | 版本与 tag | `pyproject.toml` 0.2.0；tag `v0.2.0` | CI（ruff + pytest，3.11/3.13 矩阵）绿色后打 tag |

---

## 5. 整体验收标准（v0.2 DoD）

1. **主题可换**：同一 ContentAST / ContentPackage 在 5 个主题下渲染，视觉互异且全部通过三层渲染门（`lint_html` + `lint_gzh` + 既有快照测试不回归）。
2. **框架可扩**：7 个写作框架定义齐备，ContentBrief.framework 自由选择；新增第 8 个框架仍是「放一个 md 文件」的零代码操作。
3. **发布可达**：`publisher.release(draft_id)` 走 Freepublish 接口产出 `status="published"` + `publish_id`；API 失败降级为 `degraded`（本地 HTML 收尾），与 v0.1 降级语义一致；pipeline `auto_release` 默认关闭，v0.1 行为零变化。
4. **质量可度**：`python scripts/run_evals.py` 离线输出 eval 报告；humanize / 渲染 / 框架三类 eval 全绿，回归时非零退出。
5. **工程基线**：ruff + pytest 全量绿色（含新增测试）；CI 零网络依赖不破坏。
6. **向后兼容**：v0.1 全部 fixtures、examples、checkpoint 均可被 v0.2 代码加载；`PublishResult` 等模型扩展不破坏既有 JSON。

---

## 6. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Freepublish 接口需认证号权限，无法真实验证 | N2 只能 mock 验证 | mock 优先（同 v0.1 M5 策略）；真实号验证步骤写入 docs/verification-checklist.md 增补 |
| 群发 / 发布次数配额耗尽 | 真实环境误发布 | `auto_release` 默认 False；发布属于显式人工决策，pipeline 不自动越过草稿箱 |
| 新主题 CSS 误用白名单外属性 | 渲染门误杀 | 主题只复用 default 主题已发射的属性键；N1 各主题 DoD 强制全组件门禁通过 |
| evals 语料主观性 | 阈值争议 | 阈值集中定义（`tests/evals/thresholds.py`）、可配置；语料只选「确定性可判」的极端正反例 |
| LLM 生成的 framework 字段值不在 7 框架内 | ContentBrief 校验通过但路由失败 | framework 为自由字符串（v0.1 设计），Agent 路由失败时回退 default 框架并在文档注明 |

---

## 7. v0.2 明确不做（Out of Scope，推迟至 v0.3）

- **语义标记嵌套**（`:::card` 内嵌 `:::note` 等）：解析器大改，v0.1 风险表已预告推迟；v0.2 不动 parser。
- **humanize 规则集大幅迭代**：仅随 evals 校准阈值，不新增检测维度。
- **Web UI / 复杂 CLI 交互**：入口仍是 Agent（SKILL.md）+ `scripts/`。
- **性能优化、并发、缓存、i18n**：延续 v0.1 决策。
- **监控告警（发布后数据回流）**：仅提供 `status()` 查询；群发数据统计进 v0.3。

---

## 8. 执行节奏建议

- **串行主线**：N1 → N3 → N4 → N5。
- **并行支线**：N2（正式发布）只依赖 v0.1 模型层，可与 N1 并行推进。
- **每里程碑出口**：DoD 全部勾选 + CI 绿色，再进入下一里程碑。
- **N5 收尾后**：打 tag `v0.2.0`，撰写 release notes，开启 v0.3 backlog（标记嵌套、发布监控、规则迭代）。
